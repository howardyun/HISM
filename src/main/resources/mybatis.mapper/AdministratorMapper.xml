<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.HISM.backfront.mapper.AdministratorMapper">
    <resultMap id="AdministratorMap" type="com.HISM.backfront.domain.Administrator">
    </resultMap>

    <select id="queryAdministratorById" resultMap="AdministratorMap">
        select * from administrator where adminId = #{adminId}
    </select>

</mapper>