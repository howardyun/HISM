<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.HISM.backfront.mapper.TipOffUserMapper">
    <resultMap id="tipOffUserMap" type="com.HISM.backfront.domain.TipOffUser">
        <result column="tipOffTime" jdbcType="TIMESTAMP" property="tipOffTime" javaType="java.util.Date" />
    </resultMap>


    <insert id="insertTipOff">
        insert into tipOffUser value (#{userId}, #{informerId}, #{tipOffTime, jdbcType=TIMESTAMP}, #{tipOffContent}, #{isValid})
    </insert>

    <update id="updateTipOff">
        update tipOffUser set tipOffTime = #{tipOffTime, jdbcType=TIMESTAMP}, tipOffContent = #{tipOffContent}, isValid = #{isValid}
        where userId = #{userId} and informerId = #{informerId}
    </update>

    <select id="selectTipOffByUserId" resultMap="tipOffUserMap">
        select * from tipOffUser where userId = #{userId}
    </select>

</mapper>